# Local multi-version testing, mirroring the CI matrix.
#   pip install tox && tox
[tox]
envlist = py39, py310, py311, py312, py313
isolated_build = true

[testenv]
extras = dev
commands = pytest -q

[testenv:lint]
deps = ruff
commands = ruff check flakehound tests
